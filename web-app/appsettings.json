{
  "ConnectionStrings": {
    // SQLite database file stored in the project root
    "DefaultConnection": "Data Source=shopping_predictor.db"
  },
  "PredictionApi": {
    // URL of the FastAPI py-app backend
    // Change this if you deploy FastAPI to a different host/port
    "BaseUrl": "http://localhost:8000/"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
