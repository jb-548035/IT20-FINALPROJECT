{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Debug",
      "Microsoft.EntityFrameworkCore": "Information"
    }
  },
  "PredictionApi": {
    // Override base URL for local development if needed
    "BaseUrl": "http://localhost:8000/"
  }
}
